/**
 * The inline code chip every token on /docs/api wears: one spelling of the
 * shape (radius, background, padding, monospace), so the spec's code spans, the
 * skeleton legend and the parameter-constraint badges read as one thing. Size
 * and colour are the caller's - a constraint badge is deliberately smaller and
 * dimmer than a code span in prose - but nothing else varies.
 */
export const CHIP = 'rounded bg-raised px-1.5 py-0.5 font-mono'
