{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://meta.audiosilo.app/schema/pack-people.schema.json",
  "title": "people pack",
  "description": "One data/people/<pack>.json file (or data/people/<dir>/<pack>.json once the family has gained a directory level): person records keyed by person slug. Every entry is CC0 by the person schema's license lock.",
  "type": "object",
  "additionalProperties": false,
  "required": ["entries"],
  "properties": {
    "entries": {
      "description": "The pack's entries, keyed by person slug. Canonical formatting sorts them, so the file is always in slug order.",
      "type": "object",
      "propertyNames": {
        "$ref": "common.schema.json#/$defs/slug"
      },
      "additionalProperties": {
        "$ref": "person.schema.json"
      }
    }
  }
}
